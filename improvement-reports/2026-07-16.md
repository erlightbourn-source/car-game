# Lane Rush — game-improver report 2026-07-16

**Commit shipped:** `d289f96` · **Live:** https://erlightbourn-source.github.io/car-game/ · cache `?v=37`
Working tree was clean at start; `main` was already up to date (last content commit `e30eca9`, Cosmos background). No re-audit needed — effort went to one verified content win.

## Market test (headless harness — `node tools/playtest.js`, 30 runs/cohort)
**Fairness oracle: normal 100% | easy 100% → PASS** (hard gate ≥95%, both modes). Exit 0. Numbers below are the post-change run.

| Cohort | median survival | reaches 5min | median score | max score | potholes/run |
|--------|----------------|--------------|--------------|-----------|--------------|
| toddler_3_5 | 49.3s | 0% | 604 | 1,292 | 4.9 |
| young_6_8 | 68.1s | 0% | 1,487 | 4,261 | 14.2 |
| tween_9_12 | 300s | 83% | 13,961 | 15,307 | 79.0 |
| teen_13plus | 300s | 100% | 14,372 | 15,464 | 90.2 |

- Easy-mode toddler: median 300s, 70% reach 5 min — gentle plateau holding for the youngest cohort.
- **Skill spread (teen/young median score): 9.7×** (>1.5 = score rewards skill) — healthy.
- **Average player can lose** (young reaches 5 min < 50%): true — difficulty ramp still bites.
- Economy: a typical (young) player earns ~192 coins/run this pass (harness run-to-run variance is expected — 171–241 across recent reports; not a balance regression). Priciest items: Lava paint 1600🪙 ≈ 8.3 runs; Cosmos bg 820🪙 ≈ 4.3; **Plasma light 760🪙 ≈ 4 runs** (new ceiling); Muscle design 560🪙 ≈ 2.9.

Fairness, difficulty, and content depth remain healthy (9 paints · 7 designs · **8 lights** · 10 backgrounds, plus missions/daily challenge/streak) — this run's single content addition targets the one category that had fallen behind, not a new problem.

## Research grounding
- Prior venture intel (Ada, 2026-07-05) frames the retention lever as a legible, more-distant next goal per category, not deeper punishment. Auditing the four cosmetic ladders against that lens: paint (6.6 runs) and backgrounds (4.8 runs, at the time) both had a genuinely aspirational ceiling; **lights (1.4 runs, Rose) did not** — the most engaged players could max the category almost immediately, flattening its pull. This run closes that specific gap rather than repeating the prior run's category.
- Chose a hue (teal/cyan "Plasma") not already covered by the existing 7 lights (red, ice-blue, amber-orange, lime-green, violet-purple, pearl-white, rose-pink) so it reads as a genuinely new option, not a re-tint.

## Shipped (verified)
- **`d289f96` — "Plasma" premium taillight (new top of the Lights catalog).** Teal/cyan glow, priced 760🪙 above Rose (340). **Pure cosmetic catalog data** in `js/config.js` — `LIGHTS` is consumed generically via color/emissive on the tail-light material (`js/shop.js:41`, `js/renderer.js:482`), with no per-id renderer branching (unlike `DESIGNS`, which hand-codes body geometry per id in `renderer.js:_buildBody()` — deliberately NOT touched this run, see flagged below). Zero engine/renderer code changed.
  - **Verified by harness:** fairness **100%/100% PASS** (both modes) both before and after the change, exit 0. Economy harness confirms Plasma is now the priciest light (760🪙 ≈ 4 runs — healthy, in line with backgrounds/paint). Skill spread and "average player can lose" both held.
  - Cache bumped **`?v=36 → 37`** (index.html only, matching the precedent in `e30eca9` — `sw.js`'s internal `CACHE` constant is a separate versioning scheme not touched by content-only releases).

## Flagged for human review (not built)
- **Design (car body) catalog is the one category still capped at a cheap ceiling (Muscle 560🪙, ~2.9 runs) with no easy fix.** Unlike paint/lights/backgrounds, `DESIGNS` requires hand-coded procedural geometry per id in `renderer.js:_buildBody()` (pickup bed, sport spoiler, muscle hood scoop, etc.) — adding a new tier is real renderer/engine code, not a data-only change, and the headless harness can't visually verify new geometry renders correctly. Left for an attended session where the shape can be eyeballed, not shipped blind in this unattended run. Effort S–M.
- **Tween/teen 300s measurement ceiling** (carried from 2026-07-06/09/13) — still unaddressed; changing the harness's `MAXT` invariant is change-class "verify-the-system," correctly left for an attended run.
- **Monetization / rewarded video / IAP / ads** — remains flagged (built OFF behind PR #1 at Evan's direction; needs a COPPA/Families-compliant provider). Not touched this run.

## Review verdict
**PASS.** Inline self-review (Lane Rush is prototype/MVP phase — floor = no secret leakage + safe deps, scaled per the engineering-floor phase guidance) of this run's diff: the only change is additive cosmetic catalog data plus the mandatory `?v=` bump — no engine, renderer, control, dependency, CSP, or monetization code touched. The fairness oracle was re-run after the change and held 100% in both normal and easy mode (not assumed); exit code 0. The new item lands in the healthy economy band (4 runs to afford, matching backgrounds/paint). The one category that genuinely needs work (Design/body geometry) was correctly flagged rather than risked blind. Working tree clean, `main` pushed. No revert needed.
