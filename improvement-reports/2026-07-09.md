# Lane Rush — game-improver report 2026-07-09

**Commit shipped:** `3a3b857` · **Live:** https://erlightbourn-source.github.io/car-game/ · cache `?v=34`
Fast-path run: `main` was unchanged since the 2026-07-06 pass (`e986581`), so effort went to a verified UX improvement rather than a re-audit.

## Market test (headless harness — `node tools/playtest.js`, 30 runs/cohort)
**Fairness oracle: normal 100% | easy 100% → PASS** (hard gate ≥95%, both modes). Exit 0.

| Cohort | median survival | reaches 5min | median score | max score | potholes/run |
|--------|----------------|--------------|--------------|-----------|--------------|
| toddler_3_5 | 49.5s | 0% | 644 | 1154 | 5.2 |
| young_6_8 | 78.2s | 0% | 1,951 | 5,668 | 14.9 |
| tween_9_12 | 300s | 90% | 13,589 | 16,443 | 82.5 |
| teen_13plus | 300s | 100% | 14,280 | 16,206 | 90.4 |

- Easy-mode toddler: median 300s, 70% reach 5 min — the gentle plateau does its job for the youngest cohort.
- **Skill spread (teen/young median score): 7.3× ** (>1.5 = score rewards skill) — healthy.
- **Average player can lose** (young reaches 5 min < 50%): true — the difficulty ramp still bites.
- Economy: a typical (young) player earns ~237 coins/run. Priciest items: Lava paint 1600🪙 ≈ 6.8 runs; Muscle design 560🪙 ≈ 2.4 runs; Rose light 340🪙 ≈ 1.4; Volcano bg 650🪙 ≈ 2.7.

The game is well-balanced and content-deep (9 paints · 7 designs · 7 lights · 9 backgrounds, plus missions, daily challenge, streak). No fairness or economy problem to fix this run.

## Research (source: Ada's monthly venture intel, 2026-07-05 — Game Growth Advisor / Tenjin / AppAgent, 2026)
- 2026 hyper-casual CPI is up (iOS $2.00–3.50). The win condition is **hybrid-casual**: keep the instant-play hook, add *light* meta-progression to push retention past D7 and clear LTV ≥ 1.5× CPI; target **D1 ≥ 40%**; rewarded-video-first monetization has the highest eCPM and least intrusion.
- Read against Lane Rush: the instant-play hook and light meta-progression are already present (streak, daily challenge, missions, a coin-priced cosmetic economy). The gap the intel points at is **making progression legible** so the next goal pulls the player back — which is exactly what this run's change targets. Monetization (rewarded video / IAP) stays **flagged**, not built.

## Shipped (verified)
- **`3a3b857` — Garage: coin shortfall on unaffordable items.** Locked shop items previously showed only their full price, so a young player couldn't tell whether a cosmetic was one run away or twenty. Each locked item now shows how many more coins it needs (e.g. "🪙 15 to go") plus an accessible `title` tooltip; owned/equipped items unchanged. Pure UI (`js/shop.js` + one `style.css` rule), no gameplay/economy touch. **Before → after:** locked item = bare price → price + legible distance-to-goal.
  - **Verified by observation:** harness fairness 100%/100% PASS (both modes) after the change; in-browser check confirmed all 8 locked items rendered the correct shortfall (price − coins, e.g. Sky 40 at 25 coins → "15 to go") and owned items showed none. Cache bumped `?v=33 → 34` so the live site serves the new JS/CSS (SW is network-first for HTML, cache-first per version — new `?v` = fresh fetch).

## Built this run at Evan's direction (branch + PR, not auto-deployed)
- **Hybrid-casual monetization — rewarded "double your coins".** Evan (2026-07-09, live) directed building the monetization the routine normally only flags. Delivered on branch `feat/rewarded-coins-20260709` (PR #1): an opt-in rewarded reward on the game-over screen (double this run's coins, one per run, gameplay never gated), with a CSP-safe **simulated** ad provider by default and a documented seam for a real network. Ships **OFF** (`ADS_ENABLED: false`) so a demo ad can't reach live child players; going live needs a COPPA/Families-compliant provider. Verified: fairness 100%/100% PASS; in-browser reward = exactly ×2 on completion, forfeit credits nothing, zero external network; passed independent monetization review (a HIGH input-gating bug found + fixed). See the PR for the go-live gates.

## Flagged for review (not built — needs a product decision)
- **Tween/teen 300s ceiling.** Both top cohorts hit the harness's 300s cap, so the oracle can't measure skill expression beyond 5 minutes. A future run could raise the harness cap to confirm the late-game ramp still separates skill past 5 min before touching any difficulty constant — measurement change first, no balance change proposed yet.

## Review verdict
**PASS — Grade A.** Independent re-read of this run's diff: the only change is additive UI; the harness fairness oracle held **100% in both normal and easy mode** after it (re-run post-change, not assumed); nothing risky (new mechanic / dependency / CSP / monetization) was auto-shipped; the `?v=` bump is present so the live site won't serve stale code; the claimed effect ("shows the shortfall") is real in the diff and was confirmed in-browser against the actual rendered shop. Phase-appropriate for a client-side kid game (no PII/backend). No revert needed.
