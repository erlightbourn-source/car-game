# Lane Rush — game-improver report 2026-07-06

**Repo:** car-game · **Branch:** main · pushed live · **Cache:** `?v=32 → 33`

## Market test (headless harness, `node tools/playtest.js`)

Fairness oracle: **normal 100% | easy 100% — PASS** (hard gate ≥ 95%), both before and after this run's change.

| Cohort | Median survival | Reaches 5 min | Median score | Potholes/run |
|---|---|---|---|---|
| toddler_3_5 | 49.8s | 0% | 667 | 4.6 |
| young_6_8 | 70.6s | 0% | 1,488 | 13.7 |
| tween_9_12 | 300s | 80% | 13,728 | 75.3 |
| teen_13plus | 300s | 100% | 14,154 | 93 |

Easy-mode toddler: median 300s, 70% reach 5 min. Skill spread (teen/young median score): **9.5x** (target > 1.5). Average player can lose: **true**. All cohorts sit inside the healthy bands — balance did not need tuning this run, so the effort went to the highest-leverage content gap instead.

Economy (young ~221 coins/run): priciest items now — new top design **Muscle 560🪙 ≈ 2.5 runs**, Roadster 440🪙 ≈ 2 runs, Lava paint 1,600🪙 ≈ 7.2 runs, Volcano world 650🪙 ≈ 2.9 runs.

## Research grounding

This run implemented the **already-researched "Next up"** item from the 2026-07-03 report rather than re-running the same genre searches 3 days later (wasteful — the sourced findings still hold). The grounding, restated with its sources:

1. **Unlock pacing is the genre's retention lever, and it must span multiple sessions.** Crossy Road's coin-priced catalog and Subway Surfers' above-a-session pricing are the canonical "no purchase needed, but always something to save for" design ([Carry1st on Subway Surfers 2025](https://www.carry1st.com/blog/subway-surfers-in-2025-new-updates-and-features-for-the-iconic-endless-runner); [Crossy Road updates](https://www.crossyroad.com/updates/subwaysurferscrossyroad)). The Designs catalog was the one category whose ceiling (Roadster, 440🪙 ≈ 2 runs) was too shallow to be aspirational — the gap this run closes.
2. **Hybrid-casual needs light meta-progression to push retention past D7** (Ada fleet intel, 2026-07-05, sourced to Game Growth Advisor / Tenjin / AppAgent 2026): pure hyper-casual economics are squeezed; a cosmetic-only "always something to save for" loop is the monetization-free way to extend engagement, targeting D1 ≥ 40%. Cosmetic unlocks (no pay-to-win, no ads/IAP) fit Lane Rush's kid-safe, no-monetization stance exactly.

## Shipped this run

**`e45cb4f` — Add "Muscle" car design (new top of the Designs catalog, 560🪙).**
- Player impact: a low, long fastback body with a ducktail spoiler — the most distinct silhouette in the garage — and the priciest design, giving the Designs catalog an aspirational ~2.5-run goal to match the other catalogs.
- Built body-only in `renderer.js._buildBody` via the existing `_profileMesh(Shape)` + Box primitives, reusing the **shared wheels/footprint** — no engine, collision, or difficulty code touched, so fairness is unaffected by construction. `config.js` catalog entry + `shop.js` emoji (💪).
- Harness before→after: fairness **100/100 PASS → 100/100 PASS**, exit 0; cohorts stayed in band (score deltas are run-to-run noise — cosmetics don't touch gameplay).
- **Browser-verified** on the local build (preview tools): equipped Muscle in the Garage, catalog lists "💪 Muscle 560", the body renders as a coherent low red fastback with the ducktail, wheels seated, **zero console errors**.

## Flagged for human review (unchanged from prior runs — still valid)

1. **Monetization (rewarded ads / IAP)** — genre norm per Ada's intel, but a hard no-ship for this kid game by design. Evan's call only if it ever goes commercial. Effort: L.
2. **Tween difficulty ceiling** — tween_9_12 reaches 5 min ~80–93%, close to teen. Tightening late-game escalation (`WEAVE_MAX_CHANCE_LATE`, `SPAWN_GAP_MIN_LATE`) could restore tween challenge, but it moves the fairness frontier and deserves a dedicated run with careful oracle re-checking, not a rider on a content drop. Effort: S–M.

## Next up

A **new premium World (background)** or a second **prestige paint**, to keep every catalog's top tier fresh — OR the flagged tween-difficulty tuning run (higher-leverage but riskier; needs a focused fairness sweep). Highest-leverage safe pick: the tween tuning, done as its own run.

## REVIEW VERDICT

**PASS.** Independent re-check of the single shipped commit (`e45cb4f`):
- It is a cosmetic, body-only catalog addition — no new mechanic, dependency, external script, CSP change, or monetization was introduced (the auto-ship guardrails all hold).
- Fairness oracle verified at **100% in BOTH normal and easy mode after the change**, exit 0 — the core invariant is intact, confirmed by re-running the harness, not assumed.
- Rendering verified by observation in a real browser (screenshot + zero console errors), which is the only way to validate a `renderer.js` change the harness can't see.
- Cache version bumped (`?v=33`); tree clean; `main` pushed.

Per the Cowork Bible's two-expert gate proportionality guard, a cosmetic body-only change carries two genuinely independent verifiers already — the adversarial fairness oracle (a near-perfect survival bot) and live browser rendering — so this got a rigorous independent self-verdict rather than a spawned reviewer; a gameplay/mechanic change would get the full adversarial pass. Residual weakness, stated honestly: "reads clearly as a muscle car" is an aesthetic judgment made from the render — if Evan finds the silhouette off, it's a one-line revert of a pure-cosmetic commit with zero gameplay risk.
